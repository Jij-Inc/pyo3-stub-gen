[tool.uv.workspace]
members = [
    "examples/pure",
    "examples/mixed",
    "examples/mixed_sub",
    "examples/test-dash-package",
]

[dependency-groups]
dev = [
    "pydantic>=2.11.9",
    "pyright>=1.1.400",
    "pytest>=8.3.5",
    "pytest-asyncio>=1.1.0",
    "pytest-insta>=0.3.0",
]
