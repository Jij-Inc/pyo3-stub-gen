[tool.uv.workspace]
members = [
    "examples/pure_abi3",
    "examples/pure_unlimited",
    "examples/mixed",
    "examples/mixed_sub",
    "examples/mixed_sub_multiple",
]

[dependency-groups]
dev = ["pyright>=1.1.400", "pytest>=8.3.5"]
